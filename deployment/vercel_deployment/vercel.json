


{
  "version": 2,
  "framework": null,
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": ".",
  "installCommand": "pip install -r requirements.txt",
  "devCommand": "python -m uvicorn api.index:app --reload --host 0.0.0.0 --port 8000",
  "functions": {
    "api/index.py": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "env": {
    "VERCEL": "1",
    "PYTHONPATH": "/opt/python",
    "MODEL_PATH": "/opt/python/optimized_models"
  }
}
